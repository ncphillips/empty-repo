---
label: Post
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: description
  type: textarea
  default: |-
    Test <a href="test">link</a>

    This is more text.

    <div> This is very long</div>

    Hello world "what is up?" said the goat
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Description
- name: content
  type: field_group_list
  fields:
  - name: text
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: text
  config:
    min: 
    max: 
    labelField: 
  label: Content
- name: test
  type: select
  default: []
  config:
    required: false
    options:
    - one
    - two
    - three
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Test
